# SheetAlerts skeleton

This repo is intentionally minimal.
The LLM will populate migrations, edge function, Apps Script files, CI, and tests.
Follow the checklist and set environment variables before running deploy.
